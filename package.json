{
  "name": "vika-ooe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-decap-admin.mjs",
    "prebuild": "node scripts/copy-decap-admin.mjs",
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx vercel@latest --prod",
    "deploy:preview": "npx vercel@latest"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "glightbox": "^3.3.1"
  }
}
